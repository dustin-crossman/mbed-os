stages:
  - build

before_script:
  - bash -c pwd
  - whoami
  - set

astyle:
  stage: build
  tags:
    - devops-linux
  script:
    - bash -ex cy-astyle.sh

##############################################################################
# Win10 - ARM, GCC_ARM and IAR
##############################################################################
win10-GCC_ARM-IAR-ARMC6:
  stage: build
  tags:
    - capsense
  script:
    - bash -ex -o igncr cy-build.sh
